# 🖱️ AutoClicker

A simple, lightweight AutoClicker built with Python.  
It allows you to record and replay mouse and keyboard actions with hotkeys — perfect for automating repetitive tasks or games.

---

## 🚀 Features
- 🎥 **Record** mouse clicks and/or keyboard input  
- ▶️ **Replay** actions at the press of a hotkey  
- ⚙️ **Customizable hotkeys** for recording and playback  
- ⏱️ Adjustable replay speed and repeat count  
- 🪟 Clean executable — no coding or setup required

---

## 📦 Installation

### 🧰 Option 1: Download the Prebuilt App
1. Go to the [Releases](../../releases) page.
2. Download the latest `.zip` file (contains `AutoClicker.exe`).
3. Extract it anywhere (e.g., Desktop).
4. Double-click `AutoClicker.exe` to run the app — no Python required.

> ⚠️ **Tip:** If you downloaded from the main repo instead of Releases, make sure you unzip the file first before running the `.exe` — otherwise you may see a “Failed to load Python DLL” error.

---

### 💻 Option 2: Run from Source Code (Developers)
If you want to modify or run the Python source code:

```bash
git clone https://github.com/<your-username>/AutoClicker.git
cd AutoClicker
pip install -r requirements.txt
python main.py
